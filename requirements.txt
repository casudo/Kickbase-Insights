requests==2.31.0 # For the API calls
pandas==2.1.3 # For the charts
pytz==2023.3.post1 # to hardcode the TZ, TODO: fix?
art==6.0 # for welcome message
croniter==2.0.1 # to convert cron expressions to a valid date
Flask==3.0.0
Flask-Cors==4.0.0